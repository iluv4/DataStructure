#include <stdio.h>
#include <string>
#include <cstdlib>
#pragma once
class node {
public:
	char item;
	Node* link;
};
bool Is_full() {

}
void Push(char c) {

}
linked list->node
// 1. c로 노드 만들기
 Node* dnode = malloc(1 * sizeof(Node));
if (top == NULL)
return true;

dnode->item = c;
dnode
if (is_empty())
dnode->link = NULL;
else
dnode->link = top;

// 2. top이 1 노드를 가리키도록
// 
void Pstack::init
pstack.init()
pstack.Push()
for (int i = 0; i < n; i++) {
	string anna; 
	void(char c)
		Push 
		

}

class pstack {
	int cnt;
	git add .
		git commit - m "커밋 메시지"
		git push origin main
};

