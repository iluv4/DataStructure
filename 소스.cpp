// 문자열 하나에 대해
FILE* fp = fopen("data2.txt", "r")
char str[100];
fgets()
processor 

int len(a) = ?
int preprocess(char *str) {
	int n = strlen(str)

bool isPalidrome() {
// +) 전처리
// 1. 단어 글자 수 사이에
		isPalindrome(str) :
			if (isPalidrome(str))
				printf("")
			else
				printf("결과: No\n")

				return false;

